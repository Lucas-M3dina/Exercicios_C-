algoritmo "Seleção de categoria natação"

VAR
   INT idade;

INICIO

      ESCREVA ("Digite sua idade: ");
      LEIA (idade);

      SE (idade >= 5 && idade <= 7) ENTÃO
      	ESCREVA ("Você esta na categoria Infantil A");

      SENÃO SE ( idade >= 8 && idade <= 10) ENTÃO
      	ESCREVA ("Você esta na categoria Infantil B");

      SENÃO SE ( idade >= 11 && idade <= 13 ) ENTÃO
      	ESCREVA ("Você esta na categoria Juvenil A");

      SENÃO SE ( idade >= 14 && idade <= 17 ) ENTÃO
      	ESCREVA ("Você esta na categoria Juvenil B");

      SENÃO SE ( idade >= 18 ) ENTÃO
      	ESCREVA ("Você esta na categoria Sênior");

      SENÃO
      	ESCREVA ("Essa idade não é permitida");

FIM DO ALGORITMO